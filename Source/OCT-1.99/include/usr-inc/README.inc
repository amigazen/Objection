README.inc
===========

This zoo file provides one additional header file used by OCT system
(tree.h), and some ANSI-C like header files.  The ANSI like files
end in ".h-a" to distinguish them from any files your system may
already have.  If your compiler doesn't already have these files,
then rename them to .h files.

--- brian witt (3-July-92)

